Name : SMB_Listener
Trigger : listener/smb
Imports : wso2/smb

listener smb:Listener smbListener = new({
     protocol: smb:SMB,
     host: "${smb_host}",
     secureSocket: {
         basicAuth: {
             username: "${smb_username}",
             password: "${smb_password}"
         }
     },
     port: ${smb_port},
     path: "${remote_smb_directory_location}",
     pollingInterval: ${polling_interval},
     fileNamePattern: "${file_type}"
 });
