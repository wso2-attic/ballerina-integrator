Name : SAP_Client
Trigger : client/sap
Imports : wso2/sap

sap:ProducerConfig sapProducerConfig = {
    destinationName: "${destination_name}",
    sapclient: "${sap_client}",
    username: "${username}",
    password: "${password}",
    ashost: "${ashost}",
    sysnr: "${sysnr}",
    language: "${language}"
};

sap:Producer sapProducer = new (sapProducerConfig);
